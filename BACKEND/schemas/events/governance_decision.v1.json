{
  "type": "object",
  "required": [
    "agent_id",
    "decision",
    "reason"
  ],
  "properties": {
    "agent_id": {
      "type": "string"
    },
    "decision": {
      "enum": [
        "allow",
        "deny",
        "require_approval",
        "redact"
      ]
    },
    "reason": {
      "type": "string"
    }
  }
}